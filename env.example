# Database Credentials
DB_USER=
DB_PASSWORD=
DB_NAME=

# JWT Secret Key (generate a secure random string for production)
JWT_SECRET=

# CORS Allowed Origins (comma-separated list of URLs)
# Example for production: CORS_ORIGIN=https://yourdomain.com
# Example for development: CORS_ORIGIN=http://localhost:5173,http://localhost
CORS_ORIGIN=
